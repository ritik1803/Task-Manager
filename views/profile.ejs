<<<<<<< HEAD
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10">
        <h1 class="text-4xl ml-5 mb-10 text-white bold">Task-Manager</h1>
        
        <h3 class="text-3xl mb-5 text-zinc-400">Hello, <%= user.name %></h3>
        
        <form method="post" action="/profile/create" class="mb-10 text-white" >
            <input class="block w-full px-5 py-2 bg-gray-700" type="text" placeholder="Title" name="title" required>
            <textarea class="resize-none block w-full mt-2 px-5 py-2 bg-gray-700" placeholder="Details" name="details" required></textarea>
            <input class="bg-blue-600 mt-2 px-4 py-2 cursor-pointer" type="submit" value="Create Task">
        </form>
        <a class="bg-red-600 mb-2 inline-block px-4 py-2 cursor-pointer text-white" href="/logout">Logout</a>
        <div class="task flex flex-wrap gap-3">
            <% if (tasks && tasks.length > 0) { %>
                <% tasks.forEach(task => { %>
                    <div class="task bg-gray-800 p-5 rounded">
                        <h2 class="text-2xl text-white"><%= task.title %></h2>
                        <p class="mt-2 text-white"><%= task.details %></p>
                        <a class="text-blue-400 inline-block mt-3" href="/profile/edit/<%= task._id %>">Edit</a>
                        <a class="text-zinc-400 inline-block ml-2 mt-3 px-2" href="/profile/show/<%= task._id %>">Show</a>
                        <form method="post" action="/profile/delete" class="inline-block">
                            <input type="hidden" name="id" value="<%= task._id %>">
                            <button class="text-red-400 ml-2 mt-2 w-15 rounded-md cursor-pointer" type="submit">Delete</button>
                        </form>
                        <a class="text-zinc-300 inline-block mt-3 px-2" href="/profile/download/<%= task._id %>" download>Download</a>
                    </div>
                <% }); %>
            <% } else { %>
                <p class="text-white">No tasks available</p>
            <% } %>
        </div>
    </div>
</body>
</html>
=======
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10">
        <h1 class="text-4xl ml-5 mb-10 text-white bold">Task-Manager</h1>
        
        <h3 class="text-3xl mb-5 text-zinc-400">Hello, <%= user.name %></h3>
        
        <form method="post" action="/profile/create" class="mb-10 text-white" >
            <input class="block w-full px-5 py-2 bg-gray-700" type="text" placeholder="Title" name="title" required>
            <textarea class="resize-none block w-full mt-2 px-5 py-2 bg-gray-700" placeholder="Details" name="details" required></textarea>
            <input class="bg-blue-600 mt-2 px-4 py-2 cursor-pointer" type="submit" value="Create Task">
        </form>
        <a class="bg-red-600 mb-2 inline-block px-4 py-2 cursor-pointer text-white" href="/logout">Logout</a>
        <div class="task flex flex-wrap gap-3">
            <% if (tasks && tasks.length > 0) { %>
                <% tasks.forEach(task => { %>
                    <div class="task bg-gray-800 p-5 rounded">
                        <h2 class="text-2xl text-white"><%= task.title %></h2>
                        <p class="mt-2 text-white"><%= task.details %></p>
                        <a class="text-blue-400 inline-block mt-3" href="/profile/edit/<%= task._id %>">Edit</a>
                        <a class="text-zinc-400 inline-block ml-2 mt-3 px-2" href="/profile/show/<%= task._id %>">Show</a>
                        <form method="post" action="/profile/delete" class="inline-block">
                            <input type="hidden" name="id" value="<%= task._id %>">
                            <button class="text-red-400 ml-2 mt-2 w-15 rounded-md cursor-pointer" type="submit">Delete</button>
                        </form>
                        <a class="text-zinc-300 inline-block mt-3 px-2" href="/profile/download/<%= task._id %>" download>Download</a>
                    </div>
                <% }); %>
            <% } else { %>
                <p class="text-white">No tasks available</p>
            <% } %>
        </div>
    </div>
</body>
</html>
>>>>>>> 2e84a7f4eae470a565c8e148b7cc4da2bd9f4506

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white">
    <div class="container mx-auto p-10">
        <h1 class="text-4xl mb-10">Edit Task</h1>
        <form method="post" action="/profile/edit/<%= task._id %>" class="mb-10">
            <input type="hidden" name="id" value="<%= task._id %>">
            <input class="block w-full px-5 py-2 bg-gray-700" type="text" name="title" value="<%= task.title %>" required>
            <textarea class="block w-full mt-2 px-5 py-2 bg-gray-700" name="details" required><%= task.details %></textarea>
            <input class="bg-blue-600 mt-2 px-4 py-2 cursor-pointer" type="submit" value="Update Task">
        </form>
    </div>
</body>
</html>

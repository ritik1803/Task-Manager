<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white">
    <div class="container mx-auto p-10">
        <h1 class="text-4xl mb-10">Register</h1>

        <% if (typeof error !== 'undefined' && error) { %>
            <p class="text-red-500 mb-4"><%= error %></p>
        <% } %>

        <form method="post" action="/register">
            <input class="px-3 py-2 w-full outline-none bg-transparent border-2 border-zinc-100 mb-4" type="text" name="name" value="<%= typeof name !== 'undefined' ? name : '' %>" placeholder="Name" required>
            <input class="px-3 py-2 w-full outline-none bg-transparent border-2 border-zinc-100 mb-4" type="text" name="username" value="<%= typeof username !== 'undefined' ? username : '' %>" placeholder="Username">
            <input class="px-3 py-2 w-full outline-none bg-transparent border-2 border-zinc-100 mb-4" type="text" name="email" value="<%= typeof email !== 'undefined' ? email : '' %>" placeholder="Email" required>
            <input class="px-3 py-2 w-full outline-none bg-transparent border-2 border-zinc-100 mb-4" type="password" name="password" placeholder="Password" required>
            <input class="cursor-pointer bg-blue-600 px-4 py-3 w-full" type="submit" value="Create Account">
        </form>
        <form action="/login" class="mt-4">
            <input class="cursor-pointer bg-yellow-600 px-4 py-3 w-full" type="submit" value="Go to Login">
        </form>
    </div>
</body>
</html>
